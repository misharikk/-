# Telegram Business Bot - ЭТАП 1

Telegram business-бот на Python для работы с бизнес-аккаунтами.

## Требования

- Python 3.11+
- Telegram Bot Token
- Telegram Business Account (для подключения бота через Connected Bots)

## Установка

1. Установите зависимости:

```bash
pip install -r requirements.txt
```

2. Создайте файл `.env` в корне проекта и добавьте токен бота:

```env
BOT_TOKEN=your_bot_token_here
```

Замените `your_bot_token_here` на реальный токен вашего бота, полученный от @BotFather.

## Запуск

```bash
python main.py
```

После запуска вы увидите в консоли:
```
🤖 Бот запускается...
Ожидаю business_message с бизнес-аккаунта...
```

## Структура проекта

```
.
├── main.py              # Точка входа
├── requirements.txt     # Зависимости
├── .env                # Переменные окружения (токен) - создать вручную
├── README.md           # Документация
├── VERSION             # Текущая версия бота
├── CHANGELOG.md        # История изменений
└── bot.log             # Логи (создается автоматически)
```

## Что умеет бот на ЭТАПЕ 1

1. **Обработка business_message:**
   - Получает сообщения из Telegram Business-аккаунта
   - Логирует в консоль и файл `bot.log`:
     - `📨 business_message получен`
     - `business_connection_id`
     - `chat.id`
     - текст сообщения
   - Полную структуру update (для дебага) можно увидеть, установив уровень логирования DEBUG

2. **Команда /start:**
   - Если кто-то напишет боту напрямую (обычный message), ответит "бот запущен"

3. **Логирование:**
   - Все логи пишутся в файл `bot.log` (UTF-8)
   - Все логи выводятся в консоль
   - Уровень по умолчанию: INFO

## Использованные технологии

- **python-telegram-bot 22.4** - библиотека для работы с Telegram Bot API
- **python-dotenv** - загрузка переменных окружения

## Настройка Telegram Business

1. Создайте бота через @BotFather и получите токен
2. Преобразуйте ваш Telegram аккаунт в Business аккаунт
3. Подключите бота к Business аккаунту через Settings → Business → Connected Bots
4. Напишите сообщение в Business аккаунт - оно должно появиться в логах бота

## Логи

Все логи сохраняются в файл `bot.log` и выводятся в консоль. Пример лога при получении business_message:

```
2025-11-29 12:00:00,000 - __main__ - INFO - 📨 business_message получен
2025-11-29 12:00:00,001 - __main__ - INFO -    business_connection_id: abc123...
2025-11-29 12:00:00,002 - __main__ - INFO -    chat.id: 123456789
2025-11-29 12:00:00,003 - __main__ - INFO -    текст сообщения: Привет
```

## Следующие этапы

После того, как вы убедитесь, что бот стабильно логирует business_message и business_connection_id, можно переходить к **ЭТАПУ 2** - создание чеклистов и задач.

## Версионирование и откаты

Проект использует простую схему версионирования:

- Текущая версия хранится в файле `VERSION`.
- При запуске бот пишет свою версию в логи и показывает её в ответ на `/start`.

### Как зафиксировать стабильную версию

1. Убедитесь, что код работает как нужно.
2. Обновите файл `VERSION` (например, с `0.1.0` на `0.2.0`).
3. Обновите `CHANGELOG.md`, добавив новый блок с описанием изменений.
4. Сделайте коммит:

   ```bash
   git add main.py VERSION CHANGELOG.md README.md
   git commit -m "Bump version to 0.2.0"
   ```

5. Поставьте тег:

   ```bash
   git tag v0.2.0
   ```

### Как откатиться к старой версии

Если в новой версии что-то сломалось, можно откатиться:

```bash
git checkout v0.2.0
```

После этого вы получаете код ровно той версии, которая была помечена тегом `v0.2.0`.
Можно запустить бота в этом состоянии и дождаться, пока новая версия будет исправлена.
