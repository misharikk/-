# 🧪 Как тестировать переход дня

## Быстрый тест (принудительный)

### Вариант 1: Команды для принудительного тестирования

1. **Создайте несколько задач:**
   - Отправьте боту текстовые сообщения (они станут задачами)
   - Отметьте часть задач как выполненные в чеклисте

2. **Закройте день:**
   ```
   /force_close
   ```
   - Отправится отчёт только с выполненными задачами
   - Все чеклисты удалятся
   - Невыполненные задачи останутся в состоянии

3. **Откройте новый день:**
   ```
   /force_newday
   ```
   - Создастся новый дневной чеклист
   - В нём будут только невыполненные задачи (если есть)
   - Если задач нет, добавится "улыбнуться себе в зеркало"

---

## Полный тест (автоматический переход дня)

### Сценарий для реального тестирования

1. **Подготовка:**
   - Создайте 2-3 задачи в боте
   - Отметьте 1-2 задачи как выполненные (галочка в чеклисте)
   - Оставьте 1 задачу невыполненной

2. **Установите время (если ещё не установлено):**
   ```
   /время 23:58
   ```
   - Это установит текущую дату и флаги для корректного перехода

3. **Дождитесь смены системной даты:**
   - Бот автоматически проверяет смену дня каждые **60 секунд**
   - Когда системная дата сменится, бот автоматически:
     - Закроет старый день (отчёт + удаление чеклистов)
     - Откроет новый день (создание чеклиста с невыполненными задачами)

4. **Проверьте результаты:**
   - ✅ Пришёл текстовый отчёт только с выполненными задачами
   - ✅ Все нативные чеклисты удалены
   - ✅ Создан новый дневной чеклист
   - ✅ В новом чеклисте только невыполненные задачи
   - ✅ Дата в title новая

---

## Мониторинг логов

### Просмотр логов в реальном времени:
```bash
tail -f bot.log
```

### Поиск событий перехода дня:
```bash
grep -E "Закрытие дня|Открытие нового дня|проверка смены|День закрыт|Новый день создан" bot.log
```

### Поиск ошибок:
```bash
grep -E "ERROR|Ошибка|❌" bot.log
```

### Проверка текущего состояния:
```bash
sqlite3 bot/state.db "SELECT chat_id, date, last_closed_date, last_opened_date FROM user_state;"
```

---

## Проверка идемпотентности

### Тест: повторное закрытие не должно происходить

1. Вызовите `/force_close`
2. Сразу же вызовите `/force_close` снова
3. **Ожидаемое поведение:**
   - Второй вызов не должен создать второй отчёт
   - `last_closed_date` должен защитить от дублирования

### Тест: повторное открытие не должно происходить

1. Вызовите `/force_newday`
2. Сразу же вызовите `/force_newday` снова
3. **Ожидаемое поведение:**
   - Второй вызов не должен создать второй чеклист
   - `last_opened_date` должен защитить от дублирования

---

## Проверка переноса задач

### Тест: невыполненные задачи переносятся

1. Создайте задачи:
   - "Поесть" ✅ (выполнено)
   - "Погулять" ❌ (не выполнено)
   - "Учиться" ❌ (не выполнено)

2. Закройте день: `/force_close`

3. Откройте новый день: `/force_newday`

4. **Ожидаемое поведение:**
   - В отчёте только "Поесть"
   - В новом чеклисте только "Погулять" и "Учиться"
   - Нумерация задач начинается с 1

---

## Проверка теговых чеклистов

### Тест: перенос теговых чеклистов

1. Создайте задачу с тегом:
   - "Купить молоко" → выберите тег "Дом"

2. Отметьте задачу как выполненную

3. Создайте ещё одну задачу с тегом "Дом":
   - "Починить кран" → не выполняйте

4. Закройте день: `/force_close`

5. Откройте новый день: `/force_newday`

6. **Ожидаемое поведение:**
   - В отчёте только "Купить молоко" в секции "Дом"
   - Создаётся новый теговый чеклист "Дом" с задачей "Починить кран"

---

## Проверка формата отчёта

### Ожидаемый формат:

```
**3 декабря**

[✔️] Поесть
[✔️] Погулять

**Дом**
[✔️] Купить молоко
```

### Если нет выполненных задач:

```
**3 декабря**

За сегодня нет выполненных задач.
```

---

## Устранение проблем

### Бот не реагирует на смену дня:

1. Проверьте, что job_queue настроен:
   ```bash
   grep "Настроена периодическая проверка" bot.log
   ```

2. Проверьте, что время установлено:
   ```bash
   sqlite3 bot/state.db "SELECT chat_id, time, date FROM user_state;"
   ```

3. Принудительно запустите проверку:
   - Отправьте любое сообщение боту (это инициализирует job_queue)

### Отчёт не пришёл:

1. Проверьте логи на ошибки:
   ```bash
   grep -E "Ошибка при отправке отчёта|Ошибка при закрытии дня" bot.log
   ```

2. Проверьте, что есть выполненные задачи:
   ```bash
   sqlite3 bot/state.db "SELECT tasks FROM user_state WHERE chat_id = YOUR_CHAT_ID;"
   ```

### Чеклисты не удаляются:

1. Проверьте логи:
   ```bash
   grep -E "Дневной чеклист удалён|Теговый чеклист.*удалён" bot.log
   ```

2. Возможно, чеклист уже был удалён вручную (это нормально)

---

## Команды для отладки

### Посмотреть текущее состояние пользователя:
```bash
sqlite3 bot/state.db << EOF
SELECT 
    chat_id,
    date,
    last_closed_date,
    last_opened_date,
    time,
    length(tasks) as tasks_count,
    length(tag_checklists) as tags_count
FROM user_state;
EOF
```

### Очистить состояние (ОСТОРОЖНО!):
```bash
# Удалить всех пользователей
sqlite3 bot/state.db "DELETE FROM user_state;"
```

### Посмотреть задачи пользователя:
```bash
sqlite3 bot/state.db "SELECT tasks FROM user_state WHERE chat_id = YOUR_CHAT_ID;" | python3 -m json.tool
```

---

## Чеклист для проверки

- [ ] Задачи создаются корректно
- [ ] Выполненные задачи отмечаются в чеклисте
- [ ] `/force_close` создаёт отчёт только с выполненными задачами
- [ ] Все чеклисты удаляются после закрытия дня
- [ ] `/force_newday` создаёт новый чеклист с невыполненными задачами
- [ ] Нумерация задач начинается с 1
- [ ] Теговые чеклисты переносятся корректно
- [ ] Отчёт сохраняется в `archive/YYYY-MM-DD.txt`
- [ ] Повторные вызовы команд не создают дубликаты (идемпотентность)
- [ ] Автоматический переход дня работает каждые 60 секунд

---

## Быстрый старт

```bash
# 1. Создайте задачи в боте (2-3 сообщения)
# 2. Отметьте часть как выполненные
# 3. Закройте день:
/force_close

# 4. Откройте новый день:
/force_newday

# 5. Проверьте логи:
tail -f bot.log | grep -E "День закрыт|Новый день создан|Отчёт"
```



