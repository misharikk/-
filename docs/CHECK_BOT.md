# Проверка работы бота

## 1. Проверьте, что бот запущен:

```bash
ps aux | grep "[p]ython.*main.py"
```

Если бот работает, вы увидите процесс.

## 2. Отправьте сообщение в бот

**ВАЖНО:** Сообщение должно быть отправлено в **бизнес-чат** (не в обычный личный чат с ботом).

## 3. Проверьте логи в реальном времени:

```bash
tail -f bot.log
```

Вы должны увидеть:
- `📥 Получено обновление` - когда приходит любое обновление
- `✅ business_message получено` - когда обрабатывается business_message
- `ℹ️ Получено обычное сообщение` - если это обычное сообщение (не business)

## 4. Если ничего не видно в логах:

1. Проверьте, что бот подключен к вашему бизнес-аккаунту
2. Убедитесь, что сообщение отправляется в бизнес-чат
3. Проверьте ошибки: `tail -50 bot.log | grep ERROR`

## 5. Если есть ошибки:

Перезапустите бота:
```bash
./restart_bot.sh
```

